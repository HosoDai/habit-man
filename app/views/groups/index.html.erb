<div class="row">
  <h2>所属グループ一覧</h2>
  <div>
    <% @groups.each do |group| %>
      <%= link_to group.name, group_path(group.id) %>, <%= group.owner_id %><br>
    <% end %>
  </div>
  <%= link_to "Create new group", new_group_path, class: "btn btn-primary" %>
</div>
