// Configure your import map in config/importmap.rb. Read more: https://github.com/rails/importmap-rails
import "@hotwired/turbo-rails"
import "controllers"
// import "custom/menu"
// import "custom/error_modal"
import "popper"
import "bootstrap"


// application.js
// $(document).on('ajax:complete', 'form[data-remote="true"]', function(event) {
//   var data = event.detail[0].responseJSON;
//   if (data.success) {
//     // 保存成功時の処理
//     window.location.href = data.group_path;
//   } else {
//     // 保存失敗時の処理
//     var errors = data.errors.join('<br>');
//     $('#exampleModal').modal('show'); // モーダルを表示する
//     $('#error-messages').html(errors); // エラーメッセージを表示する場所のIDを指定
//   }
// });

